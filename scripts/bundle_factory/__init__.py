"""
Bundle Factory — Actor-Critic Bundle 自動生成コアモジュール

bundle-factory.py CLI が使用する内部モジュール群。

モジュール:
    qa_strategy       — artifact_format → QA テンプレート自動選択
    bundle_blueprint  — 自然言語仕様 → Task Agent + QA Agent + bundle.json 生成
"""
